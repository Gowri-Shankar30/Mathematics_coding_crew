class Solution
{
	public:
		long long int reverse_digit(long long int n)
		{
		    // Code here
		   long long int c=0,r=0;
		    while(n)
		    {
		        r=n%10;
		        c=(c*10)+r;
		        n=n/10;
		    }
		    return c;
		}
};
